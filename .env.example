# Passcode required to unlock the app (choose your own value).
APP_PASSCODE=

# Random secret used to sign the auth cookie. Generate with:
#   openssl rand -hex 32
SESSION_SECRET=

# Provided automatically once you add a Postgres (Neon) database to this
# project in the Vercel dashboard (Storage tab) — no need to set by hand.
# DATABASE_URL=
